<div class="login-box">
    <h2>Login</h2>
    <form id="login-form">
        <div class="user-box">
          <input type="text" id="username" name="username" autocomplete="off" required />
            <section class="label_name">
              <span class="content_name">
                User
              </span>
            </section>
            
          </div>
          <div class="user-box">
            <input type="password" id="password" name="password" autocomplete="off"  required />
            <section class="label_name">
              <span class="content_name">
                Password
              </span>
            </section>
            
          </div>
        <button type="submit">Click</button>
        <p id="error-message">Puedes usar las cuentas de la documentacion :)</p>
    </form>
    
</div>

  <style>
    form{
        display: flex;
        flex-direction: column;
        justify-content: space-around;
    }

    .login-box {
        display: flex;
        flex-direction: column;
        width: 100%;
        height: 100%;
        padding: 40px;
        box-sizing: border-box;
        border-radius: 10px;
}

.login-box h2 {
  margin: 0 0 30px;
  padding: 0;
  color: #fff;
  text-align: center;
}

.user-box {
        width: 40%;
        position: relative;
        height: 60px;
        bottom: 20px;
        margin-left: auto;
        margin-right: auto;
        overflow: hidden;
        background-color: inherit;
    }

    .user-box input {
        
        width: 100%;
        height: 100%;
        padding-top: 22px;
        color: #fff;
        border: none;
        background-color: rgba(255, 255, 255, 0.0);/* Fondo blanco con 80% de opacidad */
    }

    .user-box section {
        position: absolute;
        bottom: 0px;
        left: 0px;
        width: 100%;
        height: 100%;
        pointer-events: none;
        border-bottom: 1px solid white;
    }
    .user-box section::after {
        content: "";
        position: absolute;
        bottom: -1px;
        left: 0px;
        width: 100%;
        height: 100%;
        border-bottom: 3px solid var(--search_color);
        transform: translateX(-100%);
        transition: all 0.3s ease;
    }

    .content_name {
        position: absolute;
        bottom: 0px;
        left: 0px;
        color: #ffffff3e;
        padding-bottom: 5px;
        transition: all 0.3s ease;
    }

    .user-box input:focus {
        outline: none;
    }

    .user-box input:focus + .label_name .content_name ,
    .user-box input:valid + .label_name .content_name {
        transform: translateY(-120%);
        font-size: 14px;
        left: 0px;
        color: var(--search_color);
        opacity: 0.85;
    }
    #login-form input:focus + .label_name::after,
    .search_button input:valid + .label_name::after {
        transform: translateX(0%);
    }



button {
        align-self: center;
        width: fit-content;
        min-width: 100px;
        color: #ffffff;
        padding: 10px 50px;
        border-radius: 5px;
        border: 2.5px solid #000000;
        box-shadow: 0px 0px 20px -20px;
        cursor: pointer;
        background-color: rgb(0, 0, 0);
        transition: all 0.2s ease-in-out 0ms;
        user-select: none;
        font-family: 'Poppins', sans-serif;
        font-weight: 700;
    }

    button:hover {
    background-color: #70707061;
    box-shadow: 0px 0px 20px -18px;
    }

    button:active {
    transform: scale(0.95);
    }

    @media (max-width: 748px) {
      .user-box{
        width: 90%;
        height: 100%;
      }
    }
    
  </style>

<script src="../../utils/login.ts"></script>
